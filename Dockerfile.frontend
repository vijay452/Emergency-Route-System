# Dockerfile for Frontend (Static Files)
FROM node:16-alpine

WORKDIR /app

# Copy frontend files
COPY frontend /app

# Expose port
EXPOSE 8000

# Start simple HTTP server
CMD ["npx", "http-server", "-p", "8000", "--cors"]
