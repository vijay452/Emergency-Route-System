// Runtime config for deployed frontend environments.
// Set window.ERS_API_BASE to your backend base URL (no trailing slash).
// Example: window.ERS_API_BASE = 'https://emergency-route-backend.onrender.com';
window.ERS_API_BASE = window.ERS_API_BASE || '';
